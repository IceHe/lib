function f1(pair) {
    console.log(pair[0]);
    console.log(pair[1]);
}
f1(['icehe', 666]);
function distanceFromOrigin(_a) {
    var x = _a[0], y = _a[1];
    return Math.sqrt(Math.pow(x, 2) + Math.pow(y, 2));
}
distanceFromOrigin([3, 4]);
