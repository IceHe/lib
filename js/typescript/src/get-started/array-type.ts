// Function printArray(value: Array<string>) {
// above as same as below
function printArray(value: string[]) {
  console.log(value);
}

const myArray: string[] = ['ice', 'he'];

printArray(myArray);
// PrintArray(new Array('hello', 'world'));
// above as same as below
printArray(['hello', 'world']);
