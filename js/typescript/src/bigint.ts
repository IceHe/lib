// Creating a bigint via the BigInt function
const aBigInt: bigint = BigInt(100);
// Creating a BigInt via the literal syntax
const anotherBigInt: bigint = 101n;

console.log(aBigInt);
console.log(anotherBigInt);
